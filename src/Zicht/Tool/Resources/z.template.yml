versioning:
    url: svn://zon/projecten/ips001/trunk

environments:
    staging:
        ssh:        "staging host name"
        root:       "staging root"
        db:         "staging database name"

    production:
        ssh:        "production host name"
        root:       "production root"
        db:         "production database name"

tasks:
    release.build:
        post:
#            - "if [ -d \"../vendor/\" ]; then rsync -rp ../vendor/ ./vendor; fi"
#            - "if [ -f \"composer.lock\" ]; then composer install; fi"
#            - "make sass"
