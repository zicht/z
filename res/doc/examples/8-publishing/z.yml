tasks:
    say_hello:
        args:
            what: ? "Hello"
            _what: what
        do:
            - @say

    say:
        args:
            _what: ?
        do:
            - echo "$(_what) world!"
