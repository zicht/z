tasks:
    say:
        set:
            times: ? 2
        # avoid over-exaggerated hello-saying
        unless: times > 5
        do:
            - for i in $$(seq 1 $(times)); do echo "Hello!"; done;